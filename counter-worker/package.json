{
  "name": "reviewchanthat-hits",
  "private": true,
  "version": "1.0.0",
  "description": "Self-hosted, privacy-first hit counter (Cloudflare Worker + KV) for reviewchanthat",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
