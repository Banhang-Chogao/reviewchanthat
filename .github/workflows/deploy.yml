name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: '0.163.1'
          extended: true

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Normalize image attribution
        run: python scripts/fix_attribution.py

      - name: Audit post images
        run: python scripts/audit_post_images.py

      - name: Check image deduplication
        run: python scripts/image_dedupe.py

      - name: Process images
        run: python scripts/process_images.py

      - name: Normalize AI summaries
        run: python scripts/normalize_ai_summaries.py --check

      - name: Content compliance
        run: python scripts/compliance.py --strict --no-public --report-json data/compliance-report.json

      - name: QA blog
        run: python scripts/qa_blog.py

      - name: Build Hugo
        run: hugo --minify

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
