repos:
  - repo: local
    hooks:
      - id: reviewchanthat-precommit
        name: Review Chân Thật precommit (images + compliance + QA + hugo)
        entry: bash scripts/precommit.sh
        language: system
        pass_filenames: false
        always_run: true